/*
 * util.h — String and memory utility declarations
 *
 * These are self-contained helpers with no UEFI library dependencies
 * beyond basic types — they work in the post-ExitBootServices
 * environment where Print() etc. are unavailable.
 */

#ifndef SUPERBOOT_UTIL_H
#define SUPERBOOT_UTIL_H

#include "../superboot.h"

#endif /* SUPERBOOT_UTIL_H */
